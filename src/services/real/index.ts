// Barrel degli adapter reali. Importato SOLO da services/factory.ts: come per i mock, la UI e i
// servizi applicativi non devono mai conoscere queste classi (Regola d'Oro).

export * from './MediaStorageLocalDisk';
