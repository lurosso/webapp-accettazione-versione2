# Dipendenze
node_modules/

# Build Next.js
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Test e coverage
coverage/
playwright-report/
test-results/

# Stato locale dell'applicazione (snapshot JSON, media caricati)
.data/*
!.data/uploads/
.data/uploads/*
!.data/uploads/.gitkeep

# Variabili d'ambiente (l'esempio resta versionato)
.env
.env.*
!.env.example

# Log e file di sistema
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/*
!.vscode/extensions.json

# Configurazione locale del browser di anteprima (Claude Code)
.claude/launch.json

# Materiale di lavoro delle sessioni Claude (tavole rese, screenshot, patch di scambio):
# non è codice e pesa qualche megabyte. Se una tavola diventa documentazione, va in docs/.
Claude outputs/

# Client Prisma generato da `prisma generate` (postinstall e build): non si versiona
src/generated/
